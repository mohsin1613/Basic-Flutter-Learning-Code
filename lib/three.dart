import 'package:flutter/material.dart';

class Three extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
            child: Text(
      "This is one open three page!",
      style: TextStyle(fontWeight: FontWeight.bold),
    )));
  }
}
