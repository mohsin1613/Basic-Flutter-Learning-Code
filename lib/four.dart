import 'package:flutter/material.dart';

class Four extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
            child: Text(
              "This is  open fouth page!",
              style: TextStyle(fontWeight: FontWeight.bold),
            )));
  }
}
